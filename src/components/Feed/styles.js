import { StyleSheet } from 'react-native';
import { 
    BORDER_RADIUS, 
    COLOR_APPLE_BLUE, 
    FONT_SIZE_1,
    COLOR_GRAY_4
} from '../../config/styles';

export default StyleSheet.create({
    container: {
        borderRadius: 5,
        flexDirection: 'column',
        padding: 20
    },
    titleContainer: {
    },
    title: {
        color: COLOR_GRAY_4
    }
});