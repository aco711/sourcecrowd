import { StyleSheet } from 'react-native';
import {
    COLOR_WHITE 
} from '../../config/styles';

export default StyleSheet.create({
    container: {
        padding: 20,
        backgroundColor: COLOR_WHITE,
        flex: 1
    }
});