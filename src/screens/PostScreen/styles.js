import { StyleSheet } from 'react-native';
import {
    COLOR_WHITE,
    COLOR_GRAY_4,
    FONT_SIZE_2
} from '../../config/styles';

export default StyleSheet.create({
    container: {
        backgroundColor: COLOR_WHITE,
        flex: 1
    },
});